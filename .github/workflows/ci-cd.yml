name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install Dependencies
        run: npm install
        env:
          CI: true

      - name: Run Tests
        run: |
          export PORT=5000
          export MONGODB_URI="mongodb+srv://didonggenz:1234567890@cluster1.xpc7x0j.mongodb.net/didonggenz?retryWrites=true&w=majority"
          export ACCESS_TOKEN=access_token
          export REFRESH_TOKEN=refresh_token
          export EMAIL_APP_PASSWORD=lyhcwehuacvpaynx
          export EMAIL_NAME=didonggenz@gmail.com
          export VNP_TMN_CODE=T11VL7KD
          export VNP_HASH_SECRET=BEVMLPMZEOJNULKRNEQDGCHVXLVUMRAH
          export VNP_URL="https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"
          export VNP_RETURN_URL="http://localhost:3000/vnpay_return"
          
          npm test
        env:
          CI: true

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Deploy to Heroku
        uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
          heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
          heroku_email: ${{ secrets.HEROKU_EMAIL }}
